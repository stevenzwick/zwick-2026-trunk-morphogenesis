# A note on a document referenced but not published

A comment in `imaging/bilaterality_fig3c/clean_replot/replot_bilaterality.py` refers to
`source_map.md`, which is not included. It was a per-section map from each Supplementary Data
display back to the analysis directory and notebook that produced it: an internal working document,
written for the people building the figures rather than for readers, and full of paths specific to
the authors' machines.

**Nothing analytical is missing.** Which analysis produced which display is covered by the top-level
README's figure index and by `supplementary/README.md`.
